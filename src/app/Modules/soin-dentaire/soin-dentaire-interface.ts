export interface SoinDentaire {
    date: Date;
    dents: string; // Liste des dents concernées
    acte: string; // Type d'acte dentaire (ex: nettoyage, extraction, etc.)
    horaire: string; // Horaire du rendez-vous
    matriculeF: string; 
    prothese: string; 
  }
  